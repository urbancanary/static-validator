"""static-validator MCP server.

Exposes the static-validator SDK as Model Context Protocol tools so
Claude Desktop (or any MCP-compatible client) can validate bond static
data without the user's data leaving their machine.
"""
